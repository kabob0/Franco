# Core dependencies
# No external dependencies required for this basic version

# Development dependencies
pytest==7.4.3
black==23.12.1
pylint==3.0.3

# IP Malicious Checker - Dipendenze Opzionali
# Nessuna dipendenza è obbligatoria (urllib è incluso in Python)
# Questo file è per compatibilità futura

# Opzionale: Per gestione file .env
python-dotenv>=0.20.0
